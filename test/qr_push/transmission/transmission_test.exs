defmodule QrPush.TransmissionTest do
  use ExUnit.Case

  test "follower is notified if mailbox stops normally" do
    # Send message
  end

  test "follower is notified if mailbox crashes" do
  end

  test "follower is notified if it is replaced" do
  end

  test "follower shutting down start's timeout" do
  end
end
